# Scripts — executable helpers, run only when explicitly needed

- **`hdr_glow.py`** — implements the HDR glow/bloom logo technique described
  in `references/design/glow-logo.md`. Run this only if Sinaida explicitly
  asked for glow/bloom branding in the current session — it’s opt-in, not
  part of the default build checklist.
